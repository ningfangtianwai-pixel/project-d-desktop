# Contributing to Project D

感谢关注 Project D。当前产品目标为 Project D V6.0 沉浸式 AI 桌面空间，程序内部版本仍为 `0.3.0-dev.0`，处于 V6.0 重构前稳定基线。贡献应优先保护桌面恢复、稳定性、隐私、性能和 Windows 兼容性，并遵循 `docs/ProjectD_V6.0_沉浸式AI桌面空间重构执行计划.md`。

## 贡献许可

Project D 自有源码采用 MIT License。提交贡献即表示你有权提交相关内容，并同意该贡献按仓库的 MIT License 提供。不要提交权利归属不清、违反第三方条款或无法按该许可提供的代码。

素材不因源码采用 MIT 而自动获得相同许可。图片、视频、字体、音频、角色和其他内容资源必须附带可核验的作者、来源、许可范围和证据；证据未完成的素材不得进入可公开分发清单。

## 开发准备

```powershell
pnpm install --frozen-lockfile
pnpm lint
pnpm typecheck
pnpm test
pnpm test:coverage
pnpm test:component
pnpm build
```

涉及 Electron 生命周期、桌面状态或安装包时，还应执行对应 E2E、恢复和打包验收。

## 变更原则

- 保持提交单一目的，说明行为变化、风险和验收命令。
- 文件移动、桌面图标隐藏、壁纸宿主和 Explorer 交互必须提供恢复路径。
- 新增 IPC 时遵循最小权限，校验发送方和参数。
- 新增窗口、监听器、定时器、Worker、子进程或数据库连接时同时实现释放路径。
- 联网请求必须具备显式开关、失败降级、日志脱敏和隐私说明。
- 测试覆盖成功、失败、取消、重复调用和异常退出。
- 不要把账号、支付、会员、云同步、遥测或其他产品扩展混入无关修复。

## 敏感信息

提交、Issue、截图和诊断资料不得包含真实 API Key、Token、邮箱、用户名、完整个人路径或私人文件名。先使用占位符和脱敏样本复现问题。

安全问题遵循 [SECURITY.md](SECURITY.md)，不要公开利用细节。

## 提交前检查

- 工作区不包含构建产物、安装包、个人配置或运行日志。
- 新增素材已登记，并附带授权证据或明确标记为不可分发。
- 文档没有把内部构建描述为正式公开版本。
- 相关类型检查、测试和构建命令已执行并记录结果。
