# Security Policy

## 支持范围

当前安全维护面向默认开发分支和最新受控测试版本。项目尚未作出公开发布、固定维护周期或响应时限承诺。

## 私密报告

不要在公开 Issue 中披露可被利用的漏洞、利用代码、API Key、Token、私人文件名、完整用户路径、诊断包或个人信息。

优先使用仓库的 GitHub Private Vulnerability Reporting。该入口尚待仓库所有者确认启用；在确认前，可创建一个不含漏洞细节的公开 Issue，请求维护者建立私密沟通渠道。不要在私密渠道建立前发送秘密信息或完整利用细节。

报告建议包含：

- 受影响版本与 Commit SHA。
- Windows 版本、安装方式、显示器和 DPI 环境。
- 最小复现步骤、预期结果与实际结果。
- 是否涉及文件操作、桌面恢复、凭据、更新包或权限边界。
- 已脱敏日志；不得包含真实凭据或私人文件内容。

## 安全边界

- 桌面文件动作应先形成计划并由用户确认，随后保留撤销或恢复证据。
- API Key 和 Token 不得硬编码、写入普通日志、进入诊断包或提交到 Git。
- Renderer 不应获得任意 Node.js、Shell 或文件系统能力；IPC 应遵循最小权限和输入校验。
- 壁纸、桌宠、网页和用户导入文件均按不可信输入处理。
- V4 免费版不启用生产支付、会员或账号服务，客户端也不能作为此类状态的可信判定端。
- 公开自动更新只有在可信发布源、完整性校验、代码签名和回滚机制完成后才可启用。

## 响应流程

维护者将按以下顺序处理报告：确认接收、验证复现、评定严重性、制定修复与披露方案。正式公开发布前仍需补齐安全联系人、值守安排和响应 SLA。

普通 UI 缺陷、功能建议和已脱敏的兼容性问题可以使用公开 Issue。
