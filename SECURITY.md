# Security Policy

## 支持范围

安全修复优先面向当前默认分支和最新公开测试版本。更早的快照可能不会单独获得补丁；报告中请提供版本号、Commit、Windows 版本和复现条件。

## 报告安全问题

请不要在公开 Issue 中披露可被利用的漏洞、API Key、访问令牌、私人文件名、诊断包或个人信息。

优先使用仓库的 GitHub Private Vulnerability Reporting：

`https://github.com/ningfangtianwai-pixel/project-d-desktop/security/advisories/new`

如果该入口不可用，说明私密报告通道尚未由仓库所有者启用。此时只创建一个不含漏洞细节的公开 Issue，请维护者启用私密通道；在安全通道建立前不要发送利用代码或秘密信息。启用私密报告是公开发布门禁之一。

报告建议包含：

- 受影响版本或 Commit。
- Windows 版本、显示器/DPI 环境和安装方式。
- 最小复现步骤、预期结果和实际结果。
- 影响范围，以及是否涉及文件移动、桌面恢复、凭据、更新包或权限边界。
- 已脱敏的日志；不要附带真实 API Key、Token 或私人文件内容。

## 安全边界

- Project D 的桌面文件操作应先生成计划，再由用户确认，并留下撤销/恢复记录。
- API Key 不应硬编码、写入日志、包含在诊断导出或提交到 Git。
- 客户端不能被视为支付、会员或授权状态的可信判定端。V4 免费版不启用生产支付和会员服务。
- 更新包在公开自动更新前必须具备可信发布源、完整性校验、签名与失败回滚。
- 壁纸、桌宠和用户导入文件都应按不可信输入处理；网页或脚本型内容不得默认获得 Node.js 或系统权限。

## 响应原则

维护者会先确认报告是否可复现，再确定严重性、修复范围和披露时间。当前项目没有承诺固定响应时限；正式公开发布前应补充维护联系人、值守安排和漏洞披露 SLA。

## 不属于私密漏洞报告的内容

普通 UI 缺陷、功能建议、已脱敏的崩溃反馈和不涉及安全边界的兼容性问题可以提交公开 Issue。
